# Anonymous Function or Lambdas

# A function without a name
# SYNTAX lambda argument_list : expression

#lambda x,y:y,x
# result = lambda x,y:y,x call lambda function

"""
Lambda Function doesn't have any name
i.e lambda x:print(x)

Lambda function returns a function
i.e show=lambda x,y:x+y

Lambda function can take zero or any number of argument
 but contains only one expressions
i.e lambda x,y: x+y

In lambda function there is no need to write return statement

It can contain expression and can't include statement in its body
"""
